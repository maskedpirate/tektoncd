apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: hello-world-task
spec:
  steps:
    - name: echo-hello
      image: ubuntu
      script: |
        #!/bin/bash
        echo "Hello, World from Tekton!"

